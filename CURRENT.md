# Current Sprint

## No sprint currently promoted

Sprint 9n — Today / Daily Schedule actionability — **shipped** (README Sprint 9n /
DONE.md). The `/today` view is now actionable: in-row Start / Mark done on scheduled
and overflow rows (recurrence-safe done endpoint, plan refetch), and blocked rows that
name each unfinished dependency with a workflow-status pill instead of bare `#id` links.

No vertical slice is currently promoted. Pick the next strongest themed group from the
backlog in `TODO.md` when ready. Open Today follow-ups that remain (explicitly out of
9n's scope):

- AI reordering with a "why this order" rationale — a later slice on top of the
  deterministic plan, routed through `ai/gateway.py` (suggestions only, Python-guarded).
- Calendar-aware scheduling — still on the README "do not build yet" list.
